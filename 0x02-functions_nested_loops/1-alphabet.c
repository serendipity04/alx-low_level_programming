#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - prints the alphabet
 * 
 * Description: prints the alphabet
 * 
 * Return: always 0
 */

int main(void)
{
    char c = 'a';
    char n = '\n';
    while(; c <= 'z'; c ++)
    {
        _putchar(c);
    }

    _putchar(n);

    return (0);
}
